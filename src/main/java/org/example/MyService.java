package org.example;
import static org.junit.Assert.assertEquals;
import org.junit.Test;
/*
public class MyService {


    @Test

    public void testPerformAction() {
// Create a mock of ExternalService

        SQLExample sqlExample = mock(SQLExample.class);
        // Define the behavior of the mock object
        when(sqlExampleMock.execute("input")).thenReturn("exec:input");
// Using the mock object in MyService
        MyService myService = new MyService(sqlExampleMock);
        // Test the method and verify the result
        assertEquals("exec:input", myService.performAction("input"));
// Verify that the mock object was used as expected
        verify(sqlExampleMock).execute("input");
    }
}

 */